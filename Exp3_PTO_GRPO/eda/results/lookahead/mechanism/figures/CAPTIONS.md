- **dispersion** — **Within-group dispersion of the training reward by training iteration, four arms (2x2).** (a) within-group SD (ddof=0) of the 8 candidate scores; (b) best-worst margin; (c) margin over SD with the iid-normal expectation (3.15) dotted and the shuffle-null range shaded; (d) the winner's standardized lead (best - group mean)/SD with its iid-normal expectation (1.58) dotted. K=0 solid + circle, K=5 dashed + square, colour = arm. training oracle (gpt-4o-mini). Read: K=5 raises SD and margin together (a, b) while (c) and (d) stay near the iid reference — the spread is rescaled, the winner does not stand out more. Pairing unit = the GROUP: the M=8 (PTO) / G=8 (GRPO) candidates the policy sampled at one branch point, keyed (arm, train_iter, conversation_id, branch_id, epoch) — PTO's branch_id is the trunk depth, so conversation_id is required. GRPO_LA5 is right-censored at train_iter 5 (its full budget); PTO arms and GRPO_LA0 run to 10. Tables: `dispersion_by_iter`, `dispersion_expectation`.
- **dispersion_tau** — **PTO pair yield vs tau, raw and rescaled.** (a) train_iter 1 (base policy, same in both arms); (b) all iterations 1-10 pooled. K=0 solid + circle, K=5 dashed + square (raw margins); open markers = K=0 margins x 1.53 (dotted) and K=5 margins / 1.53 (dash-dot), the iteration-1 within-group SD ratio; vertical dotted line = the tau used in training (0.1). training oracle (gpt-4o-mini). Read: the share of K=5's yield advantage the rescaled K=0 curve reproduces is the part of the 'more pairs under look-ahead' story that is a scale artefact of the absolute tau filter. Pairing unit = the GROUP: the M=8 (PTO) / G=8 (GRPO) candidates the policy sampled at one branch point, keyed (arm, train_iter, conversation_id, branch_id, epoch) — PTO's branch_id is the trunk depth, so conversation_id is required. Table: `dispersion_tau`.
- **faithfulness** — **Training-reward faithfulness — eval side graded by training oracle (gpt-4o-mini); proxy = the training oracle.** Panels (a) PTO and (b) GRPO: agreement between the proxy and the full-conversation eval Q1Q2 vs prefix length n_turns, pooled over iterations (PTO 1-10, GRPO 1-5 so the censored GRPO_LA5 is like-for-like); K=0 solid + circle, K=5 dashed + square, ribbons = 95% cluster-bootstrap CI over conversations, 0.5 = chance. Panel (c): the matched-policy (train_iter 1, same base policy in both K arms) K0 - K5 difference per bin with CI; NEGATIVE = look-ahead more faithful. GRPO_LA5 is right-censored at iteration 5 (train_iter 1..5, eval_iter 0..4). Tables: `faithfulness_curve`, `faithfulness_matched_policy`.
- **faithfulness_heldout** — **Training-reward faithfulness — eval side graded by held-out judge (Claude Haiku 4.5); proxy = the training oracle.** Panels (a) PTO and (b) GRPO: agreement between the proxy and the full-conversation eval Q1Q2 vs prefix length n_turns, pooled over iterations (PTO 1-10, GRPO 1-5 so the censored GRPO_LA5 is like-for-like); K=0 solid + circle, K=5 dashed + square, ribbons = 95% cluster-bootstrap CI over conversations, 0.5 = chance. Panel (c): the matched-policy (train_iter 1, same base policy in both K arms) K0 - K5 difference per bin with CI; NEGATIVE = look-ahead more faithful. GRPO_LA5 is right-censored at iteration 5 (train_iter 1..5, eval_iter 0..4). Tables: `faithfulness_curve_heldout`, `faithfulness_matched_policy`.
- **k_mechanism_overpraise** — **The reward-hack channel at all three levels, both PTO look-ahead arms.** Top: what the training reward selects for within a group (+/-1 SE over groups). Middle: what the policy generates over every candidate. Bottom: what the full-conversation eval measures per therapist turn (primary oracle). Training rows are shifted -1 onto the policy axis so all three describe the same policy; K=0 solid, K=5 dashed. K=5's selection weight staying flat where K=0's rises is the mechanistic claim: look-ahead changes the REWARD, and the pool and the eval follow. The top two rows are judge-invariant (they record the training oracle's own choices). Pairing unit = the GROUP: the M=8 (PTO) / G=8 (GRPO) candidates the policy sampled at one branch point, keyed (arm, train_iter, conversation_id, branch_id, epoch) — PTO's branch_id is the trunk depth, so conversation_id is required. Table: `k_mechanism_overpraise_chain`.
- **tail_audit** — **Look-ahead tails of the K=5 arms.** (a) share of candidates whose 5-turn tail ended early, by training iteration (with the patient-closed share dotted underneath — almost every early ending is the patient closing the session), 95% bootstrap CI; (b) within-group reward (score - group mean, Q1Q2 points) by realized tail length, pooled over iterations — odd = patient closed, even = ended on a therapist turn; (c) P(candidate is the group argmax) for ended-early vs full-tail candidates by iteration, 1/8 = chance. PTO_LA5 dashed + square, GRPO_LA5 as drawn in the legend; colour = arm. Grader: the training oracle (gpt-4o-mini). Pairing unit = the GROUP: the M=8 (PTO) / G=8 (GRPO) candidates the policy sampled at one branch point, keyed (arm, train_iter, conversation_id, branch_id, epoch) — PTO's branch_id is the trunk depth, so conversation_id is required. GRPO_LA5 is right-censored at train_iter 5 (its full budget); PTO arms and GRPO_LA0 run to 10. Tables: `tail_audit_by_iter`, `tail_score_by_realized_turns`, `tail_within_group`.
