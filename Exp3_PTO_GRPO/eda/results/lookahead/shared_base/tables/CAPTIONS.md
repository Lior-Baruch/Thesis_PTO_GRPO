- **agreement_by_state** — Per (state, instrument): per-conversation Pearson r between the two graders over the 21 model states (ONE Base: all 192 base conversations paired).
- **agreement_summary** — Per instrument: r at the K=5 run's iteration 10, the median r over the 21 states, the difference, and that state's rank (1 = the instrument's worst-agreeing state). Compare down a column, never across instruments.
- **cond_yield_claude-haiku-4-5** — [claude-haiku-4-5] The yield split by what the patient said BEFORE the therapist's turn: per (run, iteration, previous patient code, therapist code) n and P(next patient = CT), Wilson 95%; th_code ALL = every therapist code. A code whose yield after change talk matches ALL is riding the patient's momentum.
- **cond_yield_gpt-4o-mini** — [gpt-4o-mini] The yield split by what the patient said BEFORE the therapist's turn: per (run, iteration, previous patient code, therapist code) n and P(next patient = CT), Wilson 95%; th_code ALL = every therapist code. A code whose yield after change talk matches ALL is riding the patient's momentum.
- **ct_trajectory_claude-haiku-4-5** — [claude-haiku-4-5] Per (run, iteration, patient turn bin): change-talk / sustain-talk share of patient utterances, n, and the share of the state's conversations reaching the bin; shared Base.
- **ct_trajectory_gpt-4o-mini** — [gpt-4o-mini] Per (run, iteration, patient turn bin): change-talk / sustain-talk share of patient utterances, n, and the share of the state's conversations reaching the bin; shared Base.
- **gains** — Each run's gain over the shared Base, persona-paired (the Base's persona value = the mean of its two conversations): anchor 'last' = both runs at iteration 10; 'best_K0' = the K=0 run at its best trained iteration on Q1+Q2 under THAT judge, the K=5 run still at iteration 10. ratio_K5_over_K0 (on the K=5 rows) = K=5's gain / K=0's gain at that anchor.
- **judge_offset** — Per state: the training oracle's mean Q1+Q2 minus the held-out grader's (21 states).
- **k_contrast** — Persona-paired K contrast at iterations 1..10 per (judge, rubric): the EDA sign (mean_delta, dz = K=0 − K=5) and the paper's (delta/dz/CI _K5_minus_K0); better = the arm the contrast favours, read through lower-is-better (MICI). Holm across ITERATIONS 1..10 within (judge, rubric) — iteration 0 is one policy with one Base, so it is not a test.
- **k_persistence** — Persona-paired K=0 − K=5 on the per-conversation persistence metrics (ct_persist, ct_relapse, st_to_ct), per grader, iterations 1..10, Holm across iterations within (judge, metric); better read with ct_relapse lower-is-better.
- **k_process_paired** — Persona-paired K=0 − K=5 on every per-conversation process metric, per grader, iterations 1..10 (Holm across those iterations within (judge, metric)); better = the arm favoured, read through process.LOWER_BETTER.
- **levels_long** — Per (judge, run, rubric, iteration): n, mean, sd (ddof=1) and SE over the state's conversations, on ONE shared Base: iteration 0 is the two GRPO runs' base draws pooled (192 conversations, identical rows under both runs). Never average the two graders.
- **marker_and_length** — Per (run, iteration) on the shared Base: lex_overpraise_marker_rate (share of therapist turns with at least one of the ten over-praise patterns, mean over conversations — judge-free), conv_len (utterances), mean_turn_len (characters per therapist turn), n_th_turns, with SEs over conversations.
- **parity_claude-haiku-4-5** — [claude-haiku-4-5] MIPROC counts vs the same grader's MITI / PCT counts per state (21 states: ONE Base).
- **parity_gpt-4o-mini** — [gpt-4o-mini] MIPROC counts vs the same grader's MITI / PCT counts per state (21 states: ONE Base).
- **parity_pooled_claude-haiku-4-5** — [claude-haiku-4-5] Fisher-z pooled within-state ρ and mean |Δ| per count over the 21 states (one Base).
- **parity_pooled_gpt-4o-mini** — [gpt-4o-mini] Fisher-z pooled within-state ρ and mean |Δ| per count over the 21 states (one Base).
- **persist_levels_claude-haiku-4-5** — [claude-haiku-4-5] Per (run, iteration): mean / SE / count over conversations of the per-conversation persistence metrics (process.PERSISTENCE_LABELS) — the unit of the persona-paired contrast in k_persistence.
- **persist_levels_gpt-4o-mini** — [gpt-4o-mini] Per (run, iteration): mean / SE / count over conversations of the per-conversation persistence metrics (process.PERSISTENCE_LABELS) — the unit of the persona-paired contrast in k_persistence.
- **persistence_claude-haiku-4-5** — [claude-haiku-4-5] CHANGE-TALK PERSISTENCE, pooled over turns: per (run, iteration, previous patient code) the distribution of the patient's NEXT code. prev_code = CT: p_ct = change talk persists, p_st = relapse to sustain talk; prev_code = ST: p_ct = conversion out of sustain talk. Wilson 95% on p_ct. Shared Base.
- **persistence_gpt-4o-mini** — [gpt-4o-mini] CHANGE-TALK PERSISTENCE, pooled over turns: per (run, iteration, previous patient code) the distribution of the patient's NEXT code. prev_code = CT: p_ct = change talk persists, p_st = relapse to sustain talk; prev_code = ST: p_ct = conversion out of sustain talk. Wilson 95% on p_ct. Shared Base.
- **process_levels_claude-haiku-4-5** — [claude-haiku-4-5] Per (run, iteration): mean and SE over the state's conversations of every per-conversation process metric (process.PROCESS_METRIC_LABELS), on the shared Base (iteration 0 = 192 conversations). Opener excluded.
- **process_levels_gpt-4o-mini** — [gpt-4o-mini] Per (run, iteration): mean and SE over the state's conversations of every per-conversation process metric (process.PROCESS_METRIC_LABELS), on the shared Base (iteration 0 = 192 conversations). Opener excluded.
- **responsiveness_claude-haiku-4-5** — [claude-haiku-4-5] Per (run, iteration, preceding patient code): the distribution of the therapist's next code, shared Base.
- **responsiveness_gpt-4o-mini** — [gpt-4o-mini] Per (run, iteration, preceding patient code): the distribution of the therapist's next code, shared Base.
- **score_table_claude-haiku-4-5** — [claude-haiku-4-5] THE COMPLETE SCORE TABLE: every instrument's mean for every model — the shared Base, then the K=0 run and the K=5 run at iterations 1..10. '*' marks the better run's value at an iteration where the persona-paired K contrast clears Holm across iterations 1..10 (k_contrast; lower-is-better read for MICI). Levels are in this grader's own units.
- **score_table_gpt-4o-mini** — [gpt-4o-mini] THE COMPLETE SCORE TABLE: every instrument's mean for every model — the shared Base, then the K=0 run and the K=5 run at iterations 1..10. '*' marks the better run's value at an iteration where the persona-paired K contrast clears Holm across iterations 1..10 (k_contrast; lower-is-better read for MICI). Levels are in this grader's own units.
- **sd_by_iter** — Per (judge, metric, run, iteration): n, mean, median, sd, iqr and the ceiling shares share_ge4 / share_ge45 / share_eq5 (replication.sd_by_iter) on the shared Base (192 conversations at iteration 0).
- **sd_trend** — Per (judge, metric, run): Spearman ρ of the per-conversation SD against iteration (0..10 and 1..10) and the variance ratio of iteration 10 against the Base and against iteration 1.
- **shared_base_numbers** — Ledger of the family's quotable scalars on the shared Base (both graders), each citing its table.
- **sign_preservation** — How often the held-out grader keeps the sign of the training oracle's state contrast, by the size of the contrast (reliability.sign_preservation over state_pairs: 8 instruments × C(21,2) pairs).
- **significant_iterations** — Per (judge, rubric): the iterations where the K contrast clears Holm, split by the arm it favours (k_contrast). first_K5_lead = the first iteration whose point estimate favours K=5.
- **state_pairs** — Every pair of the 21 states × instrument under both graders, persona-paired (the Base's persona value is the mean of its two conversations): deltas, dz, the held-out side's bootstrap CI (BOOT_SEED) and same_sign.
- **text_diversity** — Per (run, iteration): template_sim (mean pairwise cosine across conversations at the same therapist turn index 1..8), persona_var_share (between-conversation / total embedding variance, 95% bootstrap over conversations), dup_rate, distinct-n, mean_words; the shared Base uses all 192 conversations.
- **text_drift** — Per (run, iteration): drift_norm = ‖therapist-turn centroid − shared Base centroid‖ (MiniLM, unit vectors), step_norm, cos_to_final. The Base centroid is the two base draws' union.
- **text_drift_cosines** — Per iteration: cosine between the two GRPO runs' displacement vectors (centroid − shared Base centroid). 1 = they moved the same way; 0 = orthogonal.
- **yield_claude-haiku-4-5** — [claude-haiku-4-5] Per (run, iteration, therapist code): n turns and the share of the patient's next utterance coded CT / ST / NEU (Wilson 95% on CT), shared Base.
- **yield_gpt-4o-mini** — [gpt-4o-mini] Per (run, iteration, therapist code): n turns and the share of the patient's next utterance coded CT / ST / NEU (Wilson 95% on CT), shared Base.
