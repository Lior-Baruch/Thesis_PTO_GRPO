- **k_retention** — **Gain retention by K, own-base reference.** Rows = Q1, Q2, MICI (harm channel); columns = PTO, GRPO. retention = Δ held-out (Claude Haiku 4.5) / Δ primary (gpt-4o-mini, the training oracle) over each arm's OWN base; K=0 solid + circle, K=5 dashed + square, ribbons = persona-bootstrap 95% CI, dash-dotted line = retention 1 (the gain is fully real to the held-out judge). Blank where |Δ primary| < 0.15 (Q1/Q2) or < 0.05 (MICI). Paired on persona_id (the trainer reshuffles the 96 personas every iteration; file_index is not a pairing key). GRPO_LA5 is right-censored at iteration 5 (its full budget); PTO arms and GRPO_LA0 run to 10. Table: `k_retention`.
