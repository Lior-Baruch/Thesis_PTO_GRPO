- **judge_agreement_scatter** — Per-conversation scores, Claude Haiku 4.5 vs the primary oracle (gpt-4o-mini), one panel per metric, all model states pooled. Distance from the dashed identity line is level bias; scatter around a trend is rank disagreement.
- **judge_contrast_preservation** — K=0 endpoint contrasts under both judges (primary oracle gpt-4o-mini vs held-out Claude Haiku 4.5). Same-sign bars mean the result does not depend on the grader also having played the patient (Claude Haiku 4.5 is a different model family). MICI: lower = better.
- **judge_saturation** — Judge saturation on Q1. (a) Per-conversation Pearson r between Claude Haiku 4.5 (held out) and the primary oracle (gpt-4o-mini, which WAS the training reward), by iteration, one line per arm; dashed = the median over all 44 judged model states (0.855). GRPO (K=5)'s late iterations hold the two lowest-agreeing states in the whole grid GRPOExp3_LA5_I9 r=0.487, GRPOExp3_LA5_I10 r=0.544. (b) The mechanism, over the SAME conversations: Q1 standard deviation by iteration for GRPO (K=5) under each grader. Both lines share one y-axis because SD is a SPREAD in each grader's own units - it is DISPERSION, not score level, that is being compared (levels differ by 1.2-1.7 points, are model-dependent, and are never compared across graders). The trained-against grader's variance falls to 0.275x of its base value while the held-out grader's rises to 1.410x, so the conversations did not become identical (that would shrink both) - one ruler saturated. (c) The same agreement at GRPOExp3_LA5_I10 across all rubrics, each against ITS OWN judged-state distribution (band = 10th-90th percentile, tick = median), since baseline agreement is not comparable across rubrics. ARM-LEVEL vs PER-CONVERSATION: every panel here is per-conversation. The arm-level sign-preservation statistic elsewhere in this family stays high - 88.4% of 7568 arm-level contrasts keep their sign (multijudge_sign_preservation) - and this figure neither overturns it nor is rescued by it: a high arm-level rate does NOT license a per-conversation claim about these states. NO MEASURED CEILING: no K=5 state carries a repeatability replicate (the anchor subset the oracle was re-scored on is K=0 only), so `ceiling` is NaN for all 176 K=5 rows and these correlations cannot be benchmarked against a measured attenuation ceiling; the judged-state median drawn in (a) is a DISTRIBUTIONAL reference, not a reliability one. Numbers: judge_saturation_data.
- **judge_saturation_grpo** — **Judge saturation, GRPO arms only** — the GRPO-scoped companion of judge_saturation (which carries all four arms and stays canonical). (a) Per-conversation Pearson r between Claude Haiku 4.5 (held out) and the primary oracle (gpt-4o-mini, which WAS the training reward) on Q1, by iteration, the two GRPO arms only; dashed = the median over the 22 GRPO model states (0.841). GRPO (K=5)'s late iterations are the lowest-agreeing GRPO states: GRPOExp3_LA5_I9 r=0.487, GRPOExp3_LA5_I10 r=0.544. (b) The mechanism, over the SAME conversations — identical to judge_saturation's panel (b), which was already GRPO_LA5-only: Q1 SD by iteration under each grader; one y-axis because SD is a SPREAD in each grader's own units (levels differ by 1.2-1.7 points and are never compared across graders). The trained-against grader's variance falls to 0.275x of its base value with a monotone trend (ρ=-0.86, p=0.001) while the held-out grader's trend is null — one ruler saturated; had the conversations become alike, both spreads would have shrunk. Scope-by-rubric lives in the table (panel c rows of judge_saturation_grpo_data). NO MEASURED CEILING: no K=5 state carries a repeatability replicate (the anchor subset the oracle was re-scored on is K=0 only), so `ceiling` is NaN for all 176 K=5 rows and these correlations cannot be benchmarked against a measured attenuation ceiling; the judged-state median drawn in (a) is a DISTRIBUTIONAL reference, not a reliability one. Numbers: judge_saturation_grpo_data.
- **multijudge_arm_means_dumbbell** — Arm means per model state (all four arms, every state with complete second-judge coverage) under the primary oracle (gpt-4o-mini) and Claude Haiku 4.5. Bar LENGTH is the level offset (large, and it cancels in every contrast); bar ORDER is what the thesis claims. Deliberately not averaged: the primary judge was the training reward and the second is held out, and the offset is model-dependent.
- **multijudge_concordance_curve** — Cross-judge ordering agreement (Claude Haiku 4.5 vs the primary oracle) vs effect size, per conversation pair. Shows how much per-conversation resolving power a given gap carries - i.e. why 96 conversations per arm are needed.
- **multijudge_gain_retention** — Gain retention under a held-out judge (Claude Haiku 4.5), all four arms' model states, with persona-bootstrap CIs. Uniform bars across arms = scale compression; one arm collapsing while others hold = that arm's gain did not transfer.
- **multijudge_retention_trajectory** — Gain retention vs iteration under Claude Haiku 4.5, one line per arm, reference PTOExp3_LA0_Base. A line near 1.0 = gains a held-out judge also sees; a line declining with training = a policy progressively fitting the grader it was trained against, with the turn point estimating when that set in. A line that ends or gaps before the axis does is retention suppressed where |delta_primary| < 0.15, not a missing model state - each arm's scored support is in multijudge_coverage.
- **multijudge_retention_trajectory_Q1** — Gain retention vs iteration under Claude Haiku 4.5, Q1 only, all four arms — the single panel the retention claim rests on, sized for a one-column figure. Same data as the Q1 panel of multijudge_retention_trajectory.
- **multijudge_variance_decomposition** — Share of arm-mean variance by source (all judged model states, primary oracle gpt-4o-mini vs the held-out judge). A large judge-level slice is harmless (it cancels in contrasts); the arm x judge slice is the only component that threatens a claim.
- **oracle_repeatability_icc** — ICC(2,1) per model and metric from 3 re-scorings of the same conversations by the primary oracle (gpt-4o-mini); dotted = Koo & Li good (0.75) / excellent (0.90).
