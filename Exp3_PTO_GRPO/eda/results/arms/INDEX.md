# Exp3 EDA artifact index — `arms/`

_Generated by `eda_analysis.build_index()`. See `SUMMARY.md` (this folder) for the written analysis and `../INDEX.md` for the map of every family._

_Each subfamily below is written by `notebooks/arms/<sub>.ipynb` and rendered once per judge (`<judge>/` leaf)._


## arms/outcomes

**Figures** — `figures/claude-haiku-4-5/`
- `effect_vs_base_forest_best.png` — Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's BEST iteration (peak Q1+Q2 (own training rubric) under claude-haiku-4-5), full-conv eval on claude-haiku-4-5; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `effect_vs_base_forest_final.png` — Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's FINAL iteration, full-conv eval on claude-haiku-4-5; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `outcomes_by_model_best.png` — Each of the four arms at its best iteration (peak Q1+Q2 (own training rubric) under claude-haiku-4-5) x metrics on claude-haiku-4-5; mean +/- 95% CI over 96 personas (the arm-bases pooled into Base; dotted line = base; MICI lower-is-better). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `outcomes_by_model_final.png` — Each of the four arms at its final iteration x metrics on claude-haiku-4-5; mean +/- 95% CI over 96 personas (the arm-bases pooled into Base; dotted line = base; MICI lower-is-better). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectories_all_metrics.png` — Full-conv eval on claude-haiku-4-5: per-metric mean +/- 95% CI across iterations, all four arms overlaid (all 9 evaluation metrics; MICI is lower-is-better). N=96 personas per model state; GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Figures** — `figures/claude-haiku-4-5/headline/`
- `effect_vs_base_forest_best.png` — Effect vs base at each arm's BEST iteration (peak Q1+Q2 (own training rubric) under claude-haiku-4-5) — headline copy; canonical: arms/outcomes/figures/<judge>/effect_vs_base_forest_best.png. Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's BEST iteration (peak Q1+Q2 (own training rubric) under claude-haiku-4-5), full-conv eval on claude-haiku-4-5; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `effect_vs_base_forest_final.png` — Effect vs base at each arm's FINAL iteration — headline copy; canonical: arms/outcomes/figures/<judge>/effect_vs_base_forest_final.png. Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's FINAL iteration, full-conv eval on claude-haiku-4-5; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectories_all_metrics.png` — THE main figure — headline copy; canonical: arms/outcomes/figures/<judge>/trajectories_all_metrics.png. Full-conv eval on claude-haiku-4-5: per-metric mean +/- 95% CI across iterations, all four arms overlaid (all 9 evaluation metrics; MICI is lower-is-better). N=96 personas per model state; GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Figures** — `figures/claude-haiku-4-5/trajectories/`
- `trajectory_CSQ-8.png` — CSQ-8 (Client Satisfaction) across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_MI-SAT.png` — MI-SAT (MI Satisfaction) across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_MICI.png` — MICI (MI-Inconsistency) ↓ across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; lower = better; no peak flag on a lower-is-better metric. GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_MITI.png` — MITI (MI Integrity) across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_PCT.png` — PCT (Patient Change-Talk) across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_Q1.png` — Q1 across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_Q1Q2.png` — Q1+Q2 across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). Grey band = oracle reproducibility (~0.10) around base. GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_Q2.png` — Q2 across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_WAI-SR.png` — WAI-SR (Working Alliance) across iterations per arm on claude-haiku-4-5 (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Figures** — `figures/gpt-4o-mini/`
- `effect_vs_base_forest_best.png` — Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's BEST iteration (peak Q1+Q2 (own training rubric) under gpt-4o-mini), full-conv eval on gpt-4o-mini; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `effect_vs_base_forest_final.png` — Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's FINAL iteration, full-conv eval on gpt-4o-mini; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `outcomes_by_model_best.png` — Each of the four arms at its best iteration (peak Q1+Q2 (own training rubric) under gpt-4o-mini) x metrics on gpt-4o-mini; mean +/- 95% CI over 96 personas (the arm-bases pooled into Base; dotted line = base; MICI lower-is-better). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `outcomes_by_model_final.png` — Each of the four arms at its final iteration x metrics on gpt-4o-mini; mean +/- 95% CI over 96 personas (the arm-bases pooled into Base; dotted line = base; MICI lower-is-better). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectories_all_metrics.png` — Full-conv eval on gpt-4o-mini: per-metric mean +/- 95% CI across iterations, all four arms overlaid (all 9 evaluation metrics; MICI is lower-is-better). N=96 personas per model state; GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Figures** — `figures/gpt-4o-mini/headline/`
- `effect_vs_base_forest_best.png` — Effect vs base at each arm's BEST iteration (peak Q1+Q2 (own training rubric) under gpt-4o-mini) — headline copy; canonical: arms/outcomes/figures/<judge>/effect_vs_base_forest_best.png. Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's BEST iteration (peak Q1+Q2 (own training rubric) under gpt-4o-mini), full-conv eval on gpt-4o-mini; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `effect_vs_base_forest_final.png` — Effect vs base at each arm's FINAL iteration — headline copy; canonical: arms/outcomes/figures/<judge>/effect_vs_base_forest_final.png. Improvement vs own base (Δ + 95% bootstrap CI) per arm x rubric at each arm's FINAL iteration, full-conv eval on gpt-4o-mini; persona-paired (96 pairs), dot color = effect-size label, dz annotated; MICI ↓ rows valence-inverted (red = worse). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectories_all_metrics.png` — THE main figure — headline copy; canonical: arms/outcomes/figures/<judge>/trajectories_all_metrics.png. Full-conv eval on gpt-4o-mini: per-metric mean +/- 95% CI across iterations, all four arms overlaid (all 9 evaluation metrics; MICI is lower-is-better). N=96 personas per model state; GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Figures** — `figures/gpt-4o-mini/trajectories/`
- `trajectory_CSQ-8.png` — CSQ-8 (Client Satisfaction) across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_MI-SAT.png` — MI-SAT (MI Satisfaction) across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_MICI.png` — MICI (MI-Inconsistency) ↓ across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; lower = better; no peak flag on a lower-is-better metric. GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_MITI.png` — MITI (MI Integrity) across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_PCT.png` — PCT (Patient Change-Talk) across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_Q1.png` — Q1 across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_Q1Q2.png` — Q1+Q2 across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). Grey band = oracle reproducibility (~0.10) around base. GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_Q2.png` — Q2 across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).
- `trajectory_WAI-SR.png` — WAI-SR (Working Alliance) across iterations per arm on gpt-4o-mini (mean +/- 95% CI, N=96 personas), all four arms; dotted vline flags a peak that precedes the final iteration (regression). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Tables** — `tables/claude-haiku-4-5/`
- `leaderboard_scorecard.md` — Final AND best iteration per arm (target column) on claude-haiku-4-5: every evaluation metric side by side (means over 96 personas; MICI lower-is-better, flagged ↓; best = peak Q1+Q2 (own training rubric) under claude-haiku-4-5). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Tables** — `tables/claude-haiku-4-5/headline/`
- `leaderboard_scorecard.md` — Scorecard (final + best) — headline copy; canonical: arms/outcomes/tables/<judge>/leaderboard_scorecard.md. Final AND best iteration per arm (target column) on claude-haiku-4-5: every evaluation metric side by side (means over 96 personas; MICI lower-is-better, flagged ↓; best = peak Q1+Q2 (own training rubric) under claude-haiku-4-5). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Tables** — `tables/gpt-4o-mini/`
- `leaderboard_scorecard.md` — Final AND best iteration per arm (target column) on gpt-4o-mini: every evaluation metric side by side (means over 96 personas; MICI lower-is-better, flagged ↓; best = peak Q1+Q2 (own training rubric) under gpt-4o-mini). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Tables** — `tables/gpt-4o-mini/headline/`
- `leaderboard_scorecard.md` — Scorecard (final + best) — headline copy; canonical: arms/outcomes/tables/<judge>/leaderboard_scorecard.md. Final AND best iteration per arm (target column) on gpt-4o-mini: every evaluation metric side by side (means over 96 personas; MICI lower-is-better, flagged ↓; best = peak Q1+Q2 (own training rubric) under gpt-4o-mini). GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Number ledgers** — `tables/claude-haiku-4-5/`
- `outcomes_numbers.json` — Ledger of the scorecard cells (keys <target>.<arm>.<metric>; best = peak Q1+Q2 (own training rubric) under claude-haiku-4-5) on claude-haiku-4-5; GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

**Number ledgers** — `tables/gpt-4o-mini/`
- `outcomes_numbers.json` — Ledger of the scorecard cells (keys <target>.<arm>.<metric>; best = peak Q1+Q2 (own training rubric) under gpt-4o-mini) on gpt-4o-mini; GRPO K=5 is censored at iteration 5 (its final = iter 5; the other arms run to iter 10).

## arms/questionnaires

**Figures** — `figures/claude-haiku-4-5/`
- `csq_detail_grid.png` — CSQ-8: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the CSQ-8 global score. Items on the 1-5 Likert scale, higher = better. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `csq_item_deltas_best.png` — Delta vs base per CSQ-8 item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `csq_item_deltas_final.png` — Delta vs base per CSQ-8 item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_detail_grid.png` — MICI severity global (1-5) + total and per-behaviour MI-inconsistent rates PER THERAPIST TURN across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid (arm means per iteration). HIGHER = WORSE. Isolates WHICH harmful move (over-praise / advise-without-permission / confront / warn / judge / persuade) carries each arm's MI-inconsistency rise. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_item_deltas_best.png` — Delta vs base per MICI component at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); HIGHER = WORSE, so + Delta = deterioration. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_item_deltas_final.png` — Delta vs base per MICI component at each arm's final iteration — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); HIGHER = WORSE, so + Delta = deterioration. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_detail_grid.png` — MI-SAT: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the MI-SAT global score. Items on the 1-5 Likert scale, higher = better. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_item_deltas_best.png` — Delta vs base per MI-SAT item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_item_deltas_final.png` — Delta vs base per MI-SAT item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_detail_grid.png` — MITI drill-down: 4 global ratings (1-5), all 7 behaviour counts PER THERAPIST TURN (length-normalized) and the derived proficiency ratios R:Q / %CR / %MICO across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid (arm means per iteration). B6_AF up while B3_Q down = the affirmation/advice drift; globals higher = better, rates are counts/turn. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_item_deltas_best.png` — Delta vs base per MITI component at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals: better; rates/ratios: more of that behaviour). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_item_deltas_final.png` — Delta vs base per MITI component at each arm's final iteration — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals: better; rates/ratios: more of that behaviour). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_proficiency_thresholds.png` — The 4 official MITI 4.2.1 summary scores (R:Q, %CR, Technical global, Relational global) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid, vs the manual's fair / good competency thresholds (dashed). Higher = more proficient; the per-arm base-vs-final verdicts are in miti_threshold_verdicts. Thresholds are expert opinion (MITI 4.2.1 manual) defined for ~20-min human sessions (short text chats are out-of-domain). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_detail_grid.png` — PCT patient-perspective globals (Importance / Confidence / Readiness, 1-5, higher = better) + change / sustain / neutral proportions of patient utterances (0-1; % Sustain lower = better) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid (arm means per iteration) — the MI target scored from the patient side. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_item_deltas_best.png` — Delta vs base per PCT component at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals / change-talk: better; % Sustain: worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_item_deltas_final.png` — Delta vs base per PCT component at each arm's final iteration — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals / change-talk: better; % Sustain: worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_detail_grid.png` — Q1: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the Q1 global score. Items on the 1-5 Likert scale, higher = better. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_item_deltas_best.png` — Delta vs base per Q1 item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_item_deltas_final.png` — Delta vs base per Q1 item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_detail_grid.png` — Q2: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the Q2 global score. Items on the 1-5 Likert scale, higher = better. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_deltas_best.png` — Delta vs base per Q2 item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). Bars coloured by our face-content item group (analytical, not a validated subscale). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_deltas_final.png` — Delta vs base per Q2 item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). Bars coloured by our face-content item group (analytical, not a validated subscale). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_group_trajectories.png` — Mean Q2 item-group score across iterations, one panel per arm (four arms; +/-95% CI over conversations x items; face-content groups — analytical, not a validated subscale) — when each reward component takes off, per arm. 1-5 Likert, higher = better. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_detail_grid.png` — WAI-SR: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the WAI-SR global score. Items on the 1-5 Likert scale, higher = better. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_item_deltas_best.png` — Delta vs base per WAI-SR item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_item_deltas_final.png` — Delta vs base per WAI-SR item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_subscales.png` — WAI-SR Goal / Task / Bond subscale means across iterations; one panel per arm (four arms; arms with <3 scored iterations omitted). 1-5 Likert, higher = better. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Figures** — `figures/claude-haiku-4-5/mici/`
- `MICI_AdviseNoPermission_rate.png` — Advise w/o permission / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Confront_rate.png` — Confront / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Direct_rate.png` — Direct/order / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Judge_rate.png` — Judge/label / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_OverPraise_rate.png` — Over-praise / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Severity.png` — MI-Incon. Severity (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Warn_rate.png` — Warn / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Figures** — `figures/claude-haiku-4-5/miti/`
- `B1_GI_per_turn.png` — Giving Information / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B2_Persuade_per_turn.png` — Persuasion / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B3_Q_per_turn.png` — Questions / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B4_SR_per_turn.png` — Simple Reflections / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B5_CR_per_turn.png` — Complex Reflections / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B6_AF_per_turn.png` — Affirmations / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B7_Seek_per_turn.png` — Seeking Collaboration / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `Empathy.png` — Empathy (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `RtoQ.png` — Reflection:Question (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Figures** — `figures/claude-haiku-4-5/pct/`
- `PCT_ChangeProp.png` — Change-Talk proportion (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_ChangeTalk_prop.png` — % Change Talk (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Confidence.png` — Confidence (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Importance.png` — Importance (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Neutral_prop.png` — % Neutral (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Readiness.png` — Readiness (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_SustainTalk_prop.png` — % Sustain Talk (PCT) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Figures** — `figures/gpt-4o-mini/`
- `csq_detail_grid.png` — CSQ-8: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the CSQ-8 global score. Items on the 1-5 Likert scale, higher = better. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `csq_item_deltas_best.png` — Delta vs base per CSQ-8 item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `csq_item_deltas_final.png` — Delta vs base per CSQ-8 item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_detail_grid.png` — MICI severity global (1-5) + total and per-behaviour MI-inconsistent rates PER THERAPIST TURN across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid (arm means per iteration). HIGHER = WORSE. Isolates WHICH harmful move (over-praise / advise-without-permission / confront / warn / judge / persuade) carries each arm's MI-inconsistency rise. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_item_deltas_best.png` — Delta vs base per MICI component at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); HIGHER = WORSE, so + Delta = deterioration. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_item_deltas_final.png` — Delta vs base per MICI component at each arm's final iteration — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); HIGHER = WORSE, so + Delta = deterioration. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_detail_grid.png` — MI-SAT: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the MI-SAT global score. Items on the 1-5 Likert scale, higher = better. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_item_deltas_best.png` — Delta vs base per MI-SAT item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_item_deltas_final.png` — Delta vs base per MI-SAT item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_detail_grid.png` — MITI drill-down: 4 global ratings (1-5), all 7 behaviour counts PER THERAPIST TURN (length-normalized) and the derived proficiency ratios R:Q / %CR / %MICO across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid (arm means per iteration). B6_AF up while B3_Q down = the affirmation/advice drift; globals higher = better, rates are counts/turn. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_item_deltas_best.png` — Delta vs base per MITI component at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals: better; rates/ratios: more of that behaviour). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_item_deltas_final.png` — Delta vs base per MITI component at each arm's final iteration — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals: better; rates/ratios: more of that behaviour). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_proficiency_thresholds.png` — The 4 official MITI 4.2.1 summary scores (R:Q, %CR, Technical global, Relational global) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid, vs the manual's fair / good competency thresholds (dashed). Higher = more proficient; the per-arm base-vs-final verdicts are in miti_threshold_verdicts. Thresholds are expert opinion (MITI 4.2.1 manual) defined for ~20-min human sessions (short text chats are out-of-domain). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_detail_grid.png` — PCT patient-perspective globals (Importance / Confidence / Readiness, 1-5, higher = better) + change / sustain / neutral proportions of patient utterances (0-1; % Sustain lower = better) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid (arm means per iteration) — the MI target scored from the patient side. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_item_deltas_best.png` — Delta vs base per PCT component at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals / change-talk: better; % Sustain: worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_item_deltas_final.png` — Delta vs base per PCT component at each arm's final iteration — one panel per arm (four arms), shared Delta scale, mixed units (see xlabel). Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals / change-talk: better; % Sustain: worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_detail_grid.png` — Q1: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the Q1 global score. Items on the 1-5 Likert scale, higher = better. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_item_deltas_best.png` — Delta vs base per Q1 item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_item_deltas_final.png` — Delta vs base per Q1 item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_detail_grid.png` — Q2: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the Q2 global score. Items on the 1-5 Likert scale, higher = better. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_deltas_best.png` — Delta vs base per Q2 item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). Bars coloured by our face-content item group (analytical, not a validated subscale). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_deltas_final.png` — Delta vs base per Q2 item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). Bars coloured by our face-content item group (analytical, not a validated subscale). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_group_trajectories.png` — Mean Q2 item-group score across iterations, one panel per arm (four arms; +/-95% CI over conversations x items; face-content groups — analytical, not a validated subscale) — when each reward component takes off, per arm. 1-5 Likert, higher = better. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_detail_grid.png` — WAI-SR: per-item mean +/- 95% CI (bootstrap over conversations) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — the item-level drill-down behind the WAI-SR global score. Items on the 1-5 Likert scale, higher = better. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_item_deltas_best.png` — Delta vs base per WAI-SR item at each arm's best iteration (own oracle) — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_item_deltas_final.png` — Delta vs base per WAI-SR item at each arm's final iteration — one panel per arm (four arms), shared Delta scale, panel title = the arm's target iteration. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score (1-5 Likert). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_subscales.png` — WAI-SR Goal / Task / Bond subscale means across iterations; one panel per arm (four arms; arms with <3 scored iterations omitted). 1-5 Likert, higher = better. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Figures** — `figures/gpt-4o-mini/mici/`
- `MICI_AdviseNoPermission_rate.png` — Advise w/o permission / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Confront_rate.png` — Confront / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Direct_rate.png` — Direct/order / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Judge_rate.png` — Judge/label / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_OverPraise_rate.png` — Over-praise / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Severity.png` — MI-Incon. Severity (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `MICI_Warn_rate.png` — Warn / turn (MICI) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-behaviour zoom of mici_detail_grid (higher = worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Figures** — `figures/gpt-4o-mini/miti/`
- `B1_GI_per_turn.png` — Giving Information / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B2_Persuade_per_turn.png` — Persuasion / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B3_Q_per_turn.png` — Questions / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B4_SR_per_turn.png` — Simple Reflections / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B5_CR_per_turn.png` — Complex Reflections / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B6_AF_per_turn.png` — Affirmations / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `B7_Seek_per_turn.png` — Seeking Collaboration / turn (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `Empathy.png` — Empathy (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `RtoQ.png` — Reflection:Question (MITI) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-metric zoom of miti_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Figures** — `figures/gpt-4o-mini/pct/`
- `PCT_ChangeProp.png` — Change-Talk proportion (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_ChangeTalk_prop.png` — % Change Talk (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Confidence.png` — Confidence (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Importance.png` — Importance (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Neutral_prop.png` — % Neutral (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_Readiness.png` — Readiness (PCT) across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `PCT_SustainTalk_prop.png` — % Sustain Talk (PCT) ↓ across iterations, four arms (PTO/GRPO x K=0/K=5) overlaid — per-signal zoom of pct_detail_grid. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Tables** — `tables/claude-haiku-4-5/`
- `csq_item_deltas.md` — Per (arm, CSQ-8 item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_behavior_by_iter.md` — Mean MICI severity (1-5) + total and per-behaviour MI-inconsistent rates per therapist turn, per (arm, iteration), all four arms; higher = worse. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_item_deltas.md` — Per (arm, MICI component): base / target values + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); HIGHER = WORSE, so + Delta = deterioration. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_item_deltas.md` — Per (arm, MI-SAT item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_detail_by_iter.md` — Mean MITI globals (1-5) + behaviour rates per therapist turn + proficiency ratios per (arm, iteration), all four arms. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_item_deltas.md` — Per (arm, MITI component): base / target values + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals: better; rates/ratios: more of that behaviour). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_threshold_verdicts.md` — Base vs final MITI 4.2.1 summary scores per arm (four arms; 'final' = that arm's last scored iteration) with the manual's competency verdict per score (check good / check fair / cross = below basic competence). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_item_deltas.md` — Per (arm, PCT component): base / target values + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals / change-talk: better; % Sustain: worse). grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_patient_by_iter.md` — Mean PCT globals (1-5) + patient-utterance proportions per (arm, iteration), all four arms. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_item_deltas.md` — Per (arm, Q1 item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_deltas.md` — Per (arm, Q2 item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_item_deltas.md` — Per (arm, WAI-SR item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: claude-haiku-4-5 (held-out judge, not the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

**Tables** — `tables/gpt-4o-mini/`
- `csq_item_deltas.md` — Per (arm, CSQ-8 item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_behavior_by_iter.md` — Mean MICI severity (1-5) + total and per-behaviour MI-inconsistent rates per therapist turn, per (arm, iteration), all four arms; higher = worse. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `mici_item_deltas.md` — Per (arm, MICI component): base / target values + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); HIGHER = WORSE, so + Delta = deterioration. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `misat_item_deltas.md` — Per (arm, MI-SAT item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_detail_by_iter.md` — Mean MITI globals (1-5) + behaviour rates per therapist turn + proficiency ratios per (arm, iteration), all four arms. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_item_deltas.md` — Per (arm, MITI component): base / target values + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals: better; rates/ratios: more of that behaviour). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `miti_threshold_verdicts.md` — Base vs final MITI 4.2.1 summary scores per arm (four arms; 'final' = that arm's last scored iteration) with the manual's competency verdict per score (check good / check fair / cross = below basic competence). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_item_deltas.md` — Per (arm, PCT component): base / target values + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher value (globals / change-talk: better; % Sustain: worse). grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `pct_patient_by_iter.md` — Mean PCT globals (1-5) + patient-utterance proportions per (arm, iteration), all four arms. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q1_item_deltas.md` — Per (arm, Q1 item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `q2_item_deltas.md` — Per (arm, Q2 item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).
- `wai_item_deltas.md` — Per (arm, WAI-SR item): base / target means + Delta at the final AND best iteration (target column), all four arms. Delta = target-iteration mean minus that arm's own iteration-0 (base) mean, a difference of arm means over the same 96 personas (not a persona-paired contrast); + = higher item score. grader: gpt-4o-mini (primary oracle = the training reward). GRPO (K=5) is right-censored at iteration 5 (the other arms run to 10).

## arms/validity

**Figures** — `figures/claude-haiku-4-5/`
- `factor_loadings.png` — Each metric's PCA loading (pooled over all four arms and every iteration; grader claude-haiku-4-5 (held-out judge)): the 5 global-eval (halo) rubrics load high on PC1 (one shared factor) and PCT loads high WITH them (Spearman rho 0.66-0.91 vs the halo rubrics under this grader); only the MITI ratios R:Q/%CR/%MICO and MICI load ~0 on PC1 and define PC2.
- `overpraise_crosscheck.png` — Per-(arm, iteration) deterministic lexical over-praise marker rate vs the oracle-coded MICI over-praise rate (grader claude-haiku-4-5 (held-out judge)), all four arms, pooled Spearman rho in the title — a directional sanity-check on the demoted regex.
- `question_rate_crosscheck.png` — Questions per therapist turn two ways, all four arms: regex literal '?' (solid) vs oracle MITI question-function (B3_Q/turn, dashed; same denominator; oracle side graded by claude-haiku-4-5 (held-out judge)). Both fall, but the regex rate collapses far more than the oracle count (base->final fold-change, base/final: GRPO_LA0: regex 5.5x, oracle 5.2x; GRPO_LA5: regex 1.1x, oracle 2.6x; PTO_LA0: regex 1.7x, oracle 2.5x; PTO_LA5: regex 1.2x, oracle 2.3x) — the widening gap = the affirmation/advice drift (declarative prompts carry question-function but no literal '?'). Syntax vs function, not a unit error. GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.
- `reward_hack_panel.png` — Twin-axis per arm (all four arms; grader claude-haiku-4-5 (held-out judge)): the global-eval reward proxy Q1+Q2 (left, 1-5) rises while MI-Inconsistency (right, higher=worse) rises with it and Patient Change-Talk (right, the actual MI goal) stays ~flat — 'all rubrics up' is not multi-skill. Per-iteration arm means over 96 personas, no CI band. GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.
- `rubric_correlation.png` — Spearman correlation among all evaluation metrics (per conversation, pooled over every iteration of all four arms; grader claude-haiku-4-5 (held-out judge)). The global-eval (halo) rubrics block together and PCT blocks WITH them (rho 0.66-0.91 vs the 5 halo rubrics under this grader), while MICI + the MITI ratios R:Q/%CR/%MICO form the genuine second family.
- `session_shape.png` — Deterministic text metrics per (arm, iteration), all four arms: mean therapist-turn length, regex ?/turn, degeneration fraction (loop), conversation length. No oracle involved (judge-invariant by construction) — the load-bearing reward-hack evidence. Per-iteration arm means over the 96 conversations (one point per arm x iteration, no CI band). GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.

**Figures** — `figures/claude-haiku-4-5/headline/`
- `reward_hack_panel.png` — The reward-hack in one frame — headline copy; canonical: arms/validity/figures/<judge>/reward_hack_panel.png.

**Figures** — `figures/gpt-4o-mini/`
- `factor_loadings.png` — Each metric's PCA loading (pooled over all four arms and every iteration; grader gpt-4o-mini (primary oracle = the training reward)): the 5 global-eval (halo) rubrics load high on PC1 (one shared factor) and PCT loads high WITH them (Spearman rho 0.79-0.94 vs the halo rubrics under this grader); only the MITI ratios R:Q/%CR/%MICO and MICI load ~0 on PC1 and define PC2.
- `overpraise_crosscheck.png` — Per-(arm, iteration) deterministic lexical over-praise marker rate vs the oracle-coded MICI over-praise rate (grader gpt-4o-mini (primary oracle = the training reward)), all four arms, pooled Spearman rho in the title — a directional sanity-check on the demoted regex.
- `question_rate_crosscheck.png` — Questions per therapist turn two ways, all four arms: regex literal '?' (solid) vs oracle MITI question-function (B3_Q/turn, dashed; same denominator; oracle side graded by gpt-4o-mini (primary oracle = the training reward)). Both fall, but the regex rate collapses far more than the oracle count (base->final fold-change, base/final: GRPO_LA0: regex 5.5x, oracle 1.4x; GRPO_LA5: regex 1.1x, oracle 1.2x; PTO_LA0: regex 1.7x, oracle 1.2x; PTO_LA5: regex 1.2x, oracle 1.3x) — the widening gap = the affirmation/advice drift (declarative prompts carry question-function but no literal '?'). Syntax vs function, not a unit error. GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.
- `reward_hack_panel.png` — Twin-axis per arm (all four arms; grader gpt-4o-mini (primary oracle = the training reward)): the global-eval reward proxy Q1+Q2 (left, 1-5) rises while MI-Inconsistency (right, higher=worse) rises with it and Patient Change-Talk (right, the actual MI goal) stays ~flat — 'all rubrics up' is not multi-skill. Per-iteration arm means over 96 personas, no CI band. GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.
- `rubric_correlation.png` — Spearman correlation among all evaluation metrics (per conversation, pooled over every iteration of all four arms; grader gpt-4o-mini (primary oracle = the training reward)). The global-eval (halo) rubrics block together and PCT blocks WITH them (rho 0.79-0.94 vs the 5 halo rubrics under this grader), while MICI + the MITI ratios R:Q/%CR/%MICO form the genuine second family.
- `session_shape.png` — Deterministic text metrics per (arm, iteration), all four arms: mean therapist-turn length, regex ?/turn, degeneration fraction (loop), conversation length. No oracle involved (judge-invariant by construction) — the load-bearing reward-hack evidence. Per-iteration arm means over the 96 conversations (one point per arm x iteration, no CI band). GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.

**Figures** — `figures/gpt-4o-mini/headline/`
- `reward_hack_panel.png` — The reward-hack in one frame — headline copy; canonical: arms/validity/figures/<judge>/reward_hack_panel.png.

**Tables** — `tables/claude-haiku-4-5/`
- `grpo_iter9_check.md` — GRPO_LA0 iter-9 anomaly (grader claude-haiku-4-5 (held-out judge)): persona-paired deltas it8->9, it9->10, it8->10 on Q1+Q2/MITI/WAI-SR (contrast label hi-lo, so + = later iteration higher; N=96 personas paired on persona_id across the per-iteration shuffle). A significant negative it9-it8 followed by a positive it10-it9 = a one-iteration dip (eval-noise or transient policy state), distinct from the monotonic Q1+Q2 reward-hack regression. Holm scope: p_holm is corrected across the 3 metrics WITHIN each contrast.
- `overpraise_crosscheck.md` — The frame behind the figure: per (arm, iteration) lexical over-praise marker rate (regex) vs oracle MICI_OverPraiseRate (grader claude-haiku-4-5 (held-out judge)); pooled Spearman rho = 0.926 across all (arm, iteration) points.
- `question_rate_crosscheck.md` — The frame behind the figure: per (arm, iteration) regex '?'/turn vs oracle MITI B3_Q/turn (grader claude-haiku-4-5 (held-out judge)), conv-aligned means over the 96 conversations. Same denominator (therapist turns); the numerators are syntax vs function.
- `rubric_pca_expanded.md` — PCA over the EXPANDED metric set (the 5 global-eval halo rubrics + PCT, MICI and the free MITI ratios R:Q/%CR/%MICO), standardized, per conversation, pooled over every iteration: PC1/PC2 variance share + PC1 loadings, pooled over all four arms and per arm; the `POOLED_halo_only` row is the 5-rubric reference (~91%). Grader claude-haiku-4-5 (held-out judge). The pooled-vs-halo-only drop is partly mechanical (more, less-correlated columns) — read as 'a second dimension exists', not as an effect size. The default-metric PC1 table lives in arms/stats (rubric_pca_pc1).
- `session_end_reasons.md` — How sessions terminate per arm, all four arms (count of conversations by session_ended_by, pooled over every scored iteration; `nan` = the session ran to the turn cap with no explicit end) — degeneration health-check. Judge-invariant (read off the transcripts). GRPO K=5 has fewer conversations because it is censored at iteration 5.
- `session_shape_by_iter.md` — Mean deterministic text metrics per (arm, iteration), all four arms: turn length (chars), regex ?/turn, loop fraction, conversation length (utterances), therapist turns. No oracle — identical under every judge. GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.

**Tables** — `tables/gpt-4o-mini/`
- `grpo_iter9_check.md` — GRPO_LA0 iter-9 anomaly (grader gpt-4o-mini (primary oracle = the training reward)): persona-paired deltas it8->9, it9->10, it8->10 on Q1+Q2/MITI/WAI-SR (contrast label hi-lo, so + = later iteration higher; N=96 personas paired on persona_id across the per-iteration shuffle). A significant negative it9-it8 followed by a positive it10-it9 = a one-iteration dip (eval-noise or transient policy state), distinct from the monotonic Q1+Q2 reward-hack regression. Holm scope: p_holm is corrected across the 3 metrics WITHIN each contrast.
- `overpraise_crosscheck.md` — The frame behind the figure: per (arm, iteration) lexical over-praise marker rate (regex) vs oracle MICI_OverPraiseRate (grader gpt-4o-mini (primary oracle = the training reward)); pooled Spearman rho = 0.903 across all (arm, iteration) points.
- `question_rate_crosscheck.md` — The frame behind the figure: per (arm, iteration) regex '?'/turn vs oracle MITI B3_Q/turn (grader gpt-4o-mini (primary oracle = the training reward)), conv-aligned means over the 96 conversations. Same denominator (therapist turns); the numerators are syntax vs function.
- `rubric_pca_expanded.md` — PCA over the EXPANDED metric set (the 5 global-eval halo rubrics + PCT, MICI and the free MITI ratios R:Q/%CR/%MICO), standardized, per conversation, pooled over every iteration: PC1/PC2 variance share + PC1 loadings, pooled over all four arms and per arm; the `POOLED_halo_only` row is the 5-rubric reference (~91%). Grader gpt-4o-mini (primary oracle = the training reward). The pooled-vs-halo-only drop is partly mechanical (more, less-correlated columns) — read as 'a second dimension exists', not as an effect size. The default-metric PC1 table lives in arms/stats (rubric_pca_pc1).
- `session_end_reasons.md` — How sessions terminate per arm, all four arms (count of conversations by session_ended_by, pooled over every scored iteration; `nan` = the session ran to the turn cap with no explicit end) — degeneration health-check. Judge-invariant (read off the transcripts). GRPO K=5 has fewer conversations because it is censored at iteration 5.
- `session_shape_by_iter.md` — Mean deterministic text metrics per (arm, iteration), all four arms: turn length (chars), regex ?/turn, loop fraction, conversation length (utterances), therapist turns. No oracle — identical under every judge. GRPO K=5 is censored at iteration 5 (its last scored iteration); PTO K=5 stops at 10 — the tail beyond is K=0 only.

## arms/heterogeneity

**Figures** — `figures/claude-haiku-4-5/`
- `cooperation_level_all_metrics.png` — [claude-haiku-4-5; held-out judge, not the training reward] All metrics by true patient cooperation_level across iterations (rows = metric, cols = arm; all four arms), each cell split by persona category — the combined overview companion to the per-metric cooperation_level/ subfolder. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `problem_all_metrics.png` — [claude-haiku-4-5; held-out judge, not the training reward] All metrics by true patient problem across iterations (rows = metric, cols = arm; all four arms), each cell split by persona category — the combined overview companion to the per-metric problem/ subfolder. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_cooperation_level_best.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1+Q2 by true patient cooperation_level at each arm's best iteration = peak Q1+Q2 (own training rubric) under claude-haiku-4-5 (GRPO_LA0 = iter 3, GRPO_LA5 = iter 5, PTO_LA0 = iter 9, PTO_LA5 = iter 7), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_cooperation_level_final.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1+Q2 by true patient cooperation_level at each arm's final iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_problem_best.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1+Q2 by true patient problem at each arm's best iteration = peak Q1+Q2 (own training rubric) under claude-haiku-4-5 (GRPO_LA0 = iter 3, GRPO_LA5 = iter 5, PTO_LA0 = iter 9, PTO_LA5 = iter 7), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_problem_final.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1+Q2 by true patient problem at each arm's final iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

**Figures** — `figures/claude-haiku-4-5/cooperation_level/`
- `CSQ-8.png` — [claude-haiku-4-5; held-out judge, not the training reward] CSQ-8 (Client Satisfaction) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MI-SAT.png` — [claude-haiku-4-5; held-out judge, not the training reward] MI-SAT (MI Satisfaction) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MICI.png` — [claude-haiku-4-5; held-out judge, not the training reward] MICI (MI-Inconsistency) ↓ across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MITI.png` — [claude-haiku-4-5; held-out judge, not the training reward] MITI (MI Integrity) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `PCT.png` — [claude-haiku-4-5; held-out judge, not the training reward] PCT (Patient Change-Talk) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1 across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1Q2.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1+Q2 across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q2.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q2 across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `WAI-SR.png` — [claude-haiku-4-5; held-out judge, not the training reward] WAI-SR (Working Alliance) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

**Figures** — `figures/claude-haiku-4-5/problem/`
- `CSQ-8.png` — [claude-haiku-4-5; held-out judge, not the training reward] CSQ-8 (Client Satisfaction) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MI-SAT.png` — [claude-haiku-4-5; held-out judge, not the training reward] MI-SAT (MI Satisfaction) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MICI.png` — [claude-haiku-4-5; held-out judge, not the training reward] MICI (MI-Inconsistency) ↓ across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MITI.png` — [claude-haiku-4-5; held-out judge, not the training reward] MITI (MI Integrity) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `PCT.png` — [claude-haiku-4-5; held-out judge, not the training reward] PCT (Patient Change-Talk) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1 across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1Q2.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q1+Q2 across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q2.png` — [claude-haiku-4-5; held-out judge, not the training reward] Q2 across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `WAI-SR.png` — [claude-haiku-4-5; held-out judge, not the training reward] WAI-SR (Working Alliance) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

**Figures** — `figures/gpt-4o-mini/`
- `cooperation_level_all_metrics.png` — [gpt-4o-mini; primary oracle = the training reward] All metrics by true patient cooperation_level across iterations (rows = metric, cols = arm; all four arms), each cell split by persona category — the combined overview companion to the per-metric cooperation_level/ subfolder. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `problem_all_metrics.png` — [gpt-4o-mini; primary oracle = the training reward] All metrics by true patient problem across iterations (rows = metric, cols = arm; all four arms), each cell split by persona category — the combined overview companion to the per-metric problem/ subfolder. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_cooperation_level_best.png` — [gpt-4o-mini; primary oracle = the training reward] Q1+Q2 by true patient cooperation_level at each arm's best iteration = peak Q1+Q2 (own training rubric) under gpt-4o-mini (GRPO_LA0 = iter 8, GRPO_LA5 = iter 4, PTO_LA0 = iter 10, PTO_LA5 = iter 10), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_cooperation_level_final.png` — [gpt-4o-mini; primary oracle = the training reward] Q1+Q2 by true patient cooperation_level at each arm's final iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_problem_best.png` — [gpt-4o-mini; primary oracle = the training reward] Q1+Q2 by true patient problem at each arm's best iteration = peak Q1+Q2 (own training rubric) under gpt-4o-mini (GRPO_LA0 = iter 8, GRPO_LA5 = iter 4, PTO_LA0 = iter 10, PTO_LA5 = iter 10), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_problem_final.png` — [gpt-4o-mini; primary oracle = the training reward] Q1+Q2 by true patient problem at each arm's final iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10), grouped bars per arm (all four arms; dotted = pooled base over all arms) — where the endpoint gap concentrates. Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

**Figures** — `figures/gpt-4o-mini/cooperation_level/`
- `CSQ-8.png` — [gpt-4o-mini; primary oracle = the training reward] CSQ-8 (Client Satisfaction) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MI-SAT.png` — [gpt-4o-mini; primary oracle = the training reward] MI-SAT (MI Satisfaction) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MICI.png` — [gpt-4o-mini; primary oracle = the training reward] MICI (MI-Inconsistency) ↓ across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MITI.png` — [gpt-4o-mini; primary oracle = the training reward] MITI (MI Integrity) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `PCT.png` — [gpt-4o-mini; primary oracle = the training reward] PCT (Patient Change-Talk) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1.png` — [gpt-4o-mini; primary oracle = the training reward] Q1 across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1Q2.png` — [gpt-4o-mini; primary oracle = the training reward] Q1+Q2 across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q2.png` — [gpt-4o-mini; primary oracle = the training reward] Q2 across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `WAI-SR.png` — [gpt-4o-mini; primary oracle = the training reward] WAI-SR (Working Alliance) across iterations split by true patient cooperation_level; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

**Figures** — `figures/gpt-4o-mini/problem/`
- `CSQ-8.png` — [gpt-4o-mini; primary oracle = the training reward] CSQ-8 (Client Satisfaction) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MI-SAT.png` — [gpt-4o-mini; primary oracle = the training reward] MI-SAT (MI Satisfaction) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MICI.png` — [gpt-4o-mini; primary oracle = the training reward] MICI (MI-Inconsistency) ↓ across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `MITI.png` — [gpt-4o-mini; primary oracle = the training reward] MITI (MI Integrity) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `PCT.png` — [gpt-4o-mini; primary oracle = the training reward] PCT (Patient Change-Talk) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1.png` — [gpt-4o-mini; primary oracle = the training reward] Q1 across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q1Q2.png` — [gpt-4o-mini; primary oracle = the training reward] Q1+Q2 across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `Q2.png` — [gpt-4o-mini; primary oracle = the training reward] Q2 across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `WAI-SR.png` — [gpt-4o-mini; primary oracle = the training reward] WAI-SR (Working Alliance) across iterations split by true patient problem; one panel per arm (all four arms). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

**Tables** — `tables/claude-haiku-4-5/`
- `subgroup_endpoint_means_cooperation_level.md` — [claude-haiku-4-5; held-out judge, not the training reward] The numbers behind the subgroup_endpoint_cooperation_level_* bars, for every metric: mean / sd / sem / n per (metric, target ∈ {final, best}, arm, cooperation_level category) at the arm's target iteration, plus the arm's OWN iteration-0 mean for that category (base_mean) and delta_vs_base = mean − base_mean (⇒ trained model higher; for MICI ↓ negative = improvement). Best = peak Q1+Q2 (own training rubric) under claude-haiku-4-5 (GRPO_LA0 = iter 3, GRPO_LA5 = iter 5, PTO_LA0 = iter 9, PTO_LA5 = iter 7); final = last scored iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_means_problem.md` — [claude-haiku-4-5; held-out judge, not the training reward] The numbers behind the subgroup_endpoint_problem_* bars, for every metric: mean / sd / sem / n per (metric, target ∈ {final, best}, arm, problem category) at the arm's target iteration, plus the arm's OWN iteration-0 mean for that category (base_mean) and delta_vs_base = mean − base_mean (⇒ trained model higher; for MICI ↓ negative = improvement). Best = peak Q1+Q2 (own training rubric) under claude-haiku-4-5 (GRPO_LA0 = iter 3, GRPO_LA5 = iter 5, PTO_LA0 = iter 9, PTO_LA5 = iter 7); final = last scored iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

**Tables** — `tables/gpt-4o-mini/`
- `subgroup_endpoint_means_cooperation_level.md` — [gpt-4o-mini; primary oracle = the training reward] The numbers behind the subgroup_endpoint_cooperation_level_* bars, for every metric: mean / sd / sem / n per (metric, target ∈ {final, best}, arm, cooperation_level category) at the arm's target iteration, plus the arm's OWN iteration-0 mean for that category (base_mean) and delta_vs_base = mean − base_mean (⇒ trained model higher; for MICI ↓ negative = improvement). Best = peak Q1+Q2 (own training rubric) under gpt-4o-mini (GRPO_LA0 = iter 8, GRPO_LA5 = iter 4, PTO_LA0 = iter 10, PTO_LA5 = iter 10); final = last scored iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).
- `subgroup_endpoint_means_problem.md` — [gpt-4o-mini; primary oracle = the training reward] The numbers behind the subgroup_endpoint_problem_* bars, for every metric: mean / sd / sem / n per (metric, target ∈ {final, best}, arm, problem category) at the arm's target iteration, plus the arm's OWN iteration-0 mean for that category (base_mean) and delta_vs_base = mean − base_mean (⇒ trained model higher; for MICI ↓ negative = improvement). Best = peak Q1+Q2 (own training rubric) under gpt-4o-mini (GRPO_LA0 = iter 8, GRPO_LA5 = iter 4, PTO_LA0 = iter 10, PTO_LA5 = iter 10); final = last scored iteration (GRPO_LA0 = iter 10, GRPO_LA5 = iter 5, PTO_LA0 = iter 10, PTO_LA5 = iter 10). Higher = better for every rubric except MICI (↓, lower = better). Unit = the 96 conversations of one model state, split by the true persona trait (categories are disjoint persona subsets, no pairing); mean per category with a 95% bootstrap CI (seed BOOT_SEED). Censoring: GRPO_LA5 ends at iteration 5 (no later checkpoint exists — its final endpoint is that iteration).

## arms/training

**Figures** — `figures/claude-haiku-4-5/`
- `faithfulness_proxy_vs_eval.png` — Per (arm, iteration), all four arms: mean TRAINING proxy reward (training oracle gpt-4o-mini on the partial branch, every candidate) vs mean full-conversation EVAL Q1+Q2 graded by claude-haiku-4-5; dashed y=x; label = eval iteration (= training iteration - 1). Points below the line = the proxy over-rates the policy. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `reward_reliability_curve.png` — Sign-agreement between the partial-conv TRAINING reward (training oracle gpt-4o-mini, chosen branch) and the full-conv EVAL Q1+Q2 as graded by claude-haiku-4-5, vs the length at which the conversation was scored (n_turns); one line per arm, all four arms. 0.5 = chance. Pairs of conversations within (arm, eval_iter, n_turns), pooled per (arm, n_turns); descriptive (LA5 above LA0 suggests look-ahead improves faithfulness -- the tested contrast is lookahead/mechanism). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).

**Figures** — `figures/gpt-4o-mini/`
- `advantage_signal_sidebyside.png` — Training decisiveness on ONE comparable oracle-score-gap scale, all four arms: the UNFILTERED best-worst candidate range (solid, both methods) is the like-for-like signal; GRPO within-group std / PTO tau-filtered chosen-rejected margin are faint secondaries. Per-branch spreads are modest and comparable (~0.2-0.3), GRPO marginally wider; PTO's tau-filtered margin sits slightly above its own range (tau keeps only large-gap branches). PTO's range declines steadily; GRPO's dips then rebounds late (K=0 iter-8 hack echo). K=5 > K=0 on both (descriptive; per-branch unit keyed on (conversation_id, branch_id)). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `faithfulness_proxy_vs_eval.png` — Per (arm, iteration), all four arms: mean TRAINING proxy reward (training oracle gpt-4o-mini on the partial branch, every candidate) vs mean full-conversation EVAL Q1+Q2 graded by gpt-4o-mini; dashed y=x; label = eval iteration (= training iteration - 1). Points below the line = the proxy over-rates the policy. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `pto_margin_by_depth.png` — PTO only (both K arms): mean chosen-rejected training-reward margin of the tau-filtered preference pairs by branch depth in the greedy trunk (deeper = longer prefix); training oracle gpt-4o-mini; unit = one emitted pair, pooled over training iterations. A margin growing with depth = the oracle discriminates more confidently on longer context.
- `reward_distribution_by_arm.png` — Per-candidate training reward per training iteration, one panel per arm (all four arms; training oracle gpt-4o-mini on partial-conv branches, NOT the full-conv eval; unit = one scored candidate). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `reward_reliability_curve.png` — Sign-agreement between the partial-conv TRAINING reward (training oracle gpt-4o-mini, chosen branch) and the full-conv EVAL Q1+Q2 as graded by gpt-4o-mini, vs the length at which the conversation was scored (n_turns); one line per arm, all four arms. 0.5 = chance. Pairs of conversations within (arm, eval_iter, n_turns), pooled per (arm, n_turns); descriptive (LA5 above LA0 suggests look-ahead improves faithfulness -- the tested contrast is lookahead/mechanism). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).

**Figures** — `figures/gpt-4o-mini/reward_distribution/`
- `GRPO_LA0.png` — GRPO (K=0): per-candidate training-reward distribution per training iteration (partial-conv training oracle gpt-4o-mini, not the full-conv eval).
- `GRPO_LA5.png` — GRPO (K=5): per-candidate training-reward distribution per training iteration (partial-conv training oracle gpt-4o-mini, not the full-conv eval).
- `PTO_LA0.png` — PTO (K=0): per-candidate training-reward distribution per training iteration (partial-conv training oracle gpt-4o-mini, not the full-conv eval).
- `PTO_LA5.png` — PTO (K=5): per-candidate training-reward distribution per training iteration (partial-conv training oracle gpt-4o-mini, not the full-conv eval).

**Figures** — `figures/gpt-4o-mini/tb_curves/`
- `GRPO_LA0.png` — GRPO (K=0): TensorBoard training curves parsed from the run's event files, chained across iterations (dotted vlines = iteration boundaries). GRPO scalars as logged by TRL (training oracle gpt-4o-mini; not judge-swappable). 
- `GRPO_LA5.png` — GRPO (K=5): TensorBoard training curves parsed from the run's event files, chained across iterations (dotted vlines = iteration boundaries). GRPO scalars as logged by TRL (training oracle gpt-4o-mini; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `PTO_LA0.png` — PTO (K=0): TensorBoard training curves parsed from the run's event files, chained across iterations (dotted vlines = iteration boundaries). DPO scalars as logged by TRL (training oracle gpt-4o-mini; not judge-swappable). 
- `PTO_LA5.png` — PTO (K=5): TensorBoard training curves parsed from the run's event files, chained across iterations (dotted vlines = iteration boundaries). DPO scalars as logged by TRL (training oracle gpt-4o-mini; not judge-swappable). 

**Tables** — `tables/claude-haiku-4-5/`
- `faithfulness_proxy_vs_eval.md` — Per (arm, eval iteration): mean training proxy reward (all candidates of training iteration N = eval iteration N-1; training oracle gpt-4o-mini) vs mean full-conv eval Q1+Q2 graded by claude-haiku-4-5, and their difference (> 0 = the proxy over-rates the policy). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `reward_reliability_by_nturns.md` — Rank agreement (fraction of conversation pairs whose training-proxy ordering matches the full-conv eval ordering, eval graded by claude-haiku-4-5) per scored length n_turns, one column per arm; 0.5 = chance. Long form with n_pairs in reward_reliability_by_nturns_long. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `reward_reliability_by_nturns_long.md` — Long form of reward_reliability_by_nturns: (arm, n_turns, agreement, n_pairs); eval graded by claude-haiku-4-5; n_pairs counts every within-(arm, eval_iter, n_turns) conversation pair, so it overstates independent information. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).

**Tables** — `tables/gpt-4o-mini/`
- `advantage_signal_by_iter.md` — Per (arm, training iteration) decisiveness signal: group_range = mean unfiltered best-worst candidate reward per branch (both methods, like-for-like); GRPO group_std / frac_zero_std (within-group spread, collapsed-group share); PTO margin / margin_median / n_pairs from the tau-filtered pairs.csv (the DPO signal). Training oracle gpt-4o-mini. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `degeneration_scan.md` — Per (arm, training iteration): candidate count and degeneration rates -- % ChatML-marker leaks, % empty-after-clean, % floored to REWARD_FLOOR -- plus mean training reward and mean completion length (chars). Near-zero = the 2026-06-07 stop-string fixes held. Training oracle gpt-4o-mini. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `faithfulness_proxy_vs_eval.md` — Per (arm, eval iteration): mean training proxy reward (all candidates of training iteration N = eval iteration N-1; training oracle gpt-4o-mini) vs mean full-conv eval Q1+Q2 graded by gpt-4o-mini, and their difference (> 0 = the proxy over-rates the policy). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `pto_margin_by_depth.md` — PTO only: per (arm, branch_depth) count of tau-filtered pairs and mean / median / sd of the chosen-rejected training-reward margin, pooled over training iterations (training oracle gpt-4o-mini).
- `reward_distribution_by_iter.md` — Per (arm, training iteration): candidate-count, mean/sd/deciles of the per-candidate training reward, and % of candidates at the reward floor (training oracle gpt-4o-mini on partial-conv branches; the numbers behind reward_distribution_by_arm). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `reward_reliability_by_nturns.md` — Rank agreement (fraction of conversation pairs whose training-proxy ordering matches the full-conv eval ordering, eval graded by gpt-4o-mini) per scored length n_turns, one column per arm; 0.5 = chance. Long form with n_pairs in reward_reliability_by_nturns_long. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).
- `reward_reliability_by_nturns_long.md` — Long form of reward_reliability_by_nturns: (arm, n_turns, agreement, n_pairs); eval graded by gpt-4o-mini; n_pairs counts every within-(arm, eval_iter, n_turns) conversation pair, so it overstates independent information. GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).

**Number ledgers** — `tables/claude-haiku-4-5/`
- `training_numbers.json` — Number ledger for arms/training: per-arm anchors -- decisiveness means (§2), degeneration maxima (§3), rank agreement at the shortest/longest scored cut (§4) and mean proxy-eval gap (§5); eval side graded by claude-haiku-4-5, training side always the training oracle gpt-4o-mini (§2/§3 keys present only under the primary leaf). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).

**Number ledgers** — `tables/gpt-4o-mini/`
- `training_numbers.json` — Number ledger for arms/training: per-arm anchors -- decisiveness means (§2), degeneration maxima (§3), rank agreement at the shortest/longest scored cut (§4) and mean proxy-eval gap (§5); eval side graded by gpt-4o-mini, training side always the training oracle gpt-4o-mini (§2/§3 keys present only under the primary leaf). GRPO K=5 stopped after iteration 5 (censored; iteration_6/ is a one-step stub).

## arms/preference

**Figures** — `figures/gpt-4o-mini/`
- `PTO_LA0_pref_category_drift.png` — PTO_LA0 MI-concept word groups projected onto the chosen-rejected direction per training iteration (+ = more preferred); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA0_pref_direction_drift.png` — PTO_LA0 preference direction in 2D PCA + consecutive cosine (how the preferred axis re-orients across training iterations; per-iteration directions, split-half ~0.19, so read as noise-dominated); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA0_pref_learn_unlearn.png` — PTO_LA0 words most newly preferred (learned) vs dropped (unlearned) across consecutive training-iteration transitions; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA0_pref_word_drift.png` — PTO_LA0 per-iteration projection of the top chosen-/rejected-aligned words (drift; per-iteration directions, see the reliability caveat -- mostly estimation noise); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA0_pref_word_ranking.png` — PTO_LA0 top chosen/rejected-aligned words (Mass Mean Probe, pooled over training iterations; + = chosen-aligned); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA5_pref_category_drift.png` — PTO_LA5 MI-concept word groups projected onto the chosen-rejected direction per training iteration (+ = more preferred); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA5_pref_direction_drift.png` — PTO_LA5 preference direction in 2D PCA + consecutive cosine (how the preferred axis re-orients across training iterations; per-iteration directions, split-half ~0.19, so read as noise-dominated); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA5_pref_learn_unlearn.png` — PTO_LA5 words most newly preferred (learned) vs dropped (unlearned) across consecutive training-iteration transitions; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA5_pref_word_drift.png` — PTO_LA5 per-iteration projection of the top chosen-/rejected-aligned words (drift; per-iteration directions, see the reliability caveat -- mostly estimation noise); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA5_pref_word_ranking.png` — PTO_LA5 top chosen/rejected-aligned words (Mass Mean Probe, pooled over training iterations; + = chosen-aligned); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `generation_vs_selection.png` — Top row: what the policy produces (unweighted mean over every candidate). Bottom row: what the update pushes toward within a group (+ = favours more of the feature). All four arms. A large gap in scale between the rows means the per-step selection pressure is small while the generated distribution moves a long way -- a compounding on-policy loop rather than one hard pull; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `pref_category_K0_vs_K5.png` — PTO MI-concept preference projection across training iterations, K=0 vs K=5 -- does look-ahead change what the policy prefers? Descriptive overlay of per-iteration pairs.csv directions (see the reliability caveat and the measured split-half in section 3); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). The tested K contrasts are in lookahead/*.
- `pref_outcome_w_affirm.png` — Update feature `w_affirm` (what the training reward selected for, training oracle gpt-4o-mini) against the persona-paired eval delta of the same iteration (primary oracle, N=96 personas), one point per training iteration (label = iteration), per arm, all four arms. Dashed = per-arm least squares, corner text = raw Spearman rho; the partial-correlation version is in pref_outcome_correlations. GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `pref_outcome_w_overpraise.png` — Update feature `w_overpraise` (what the training reward selected for, training oracle gpt-4o-mini) against the persona-paired eval delta of the same iteration (primary oracle, N=96 personas), one point per training iteration (label = iteration), per arm, all four arms. Dashed = per-arm least squares, corner text = raw Spearman rho; the partial-correlation version is in pref_outcome_correlations. GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `pref_outcome_w_question.png` — Update feature `w_question` (what the training reward selected for, training oracle gpt-4o-mini) against the persona-paired eval delta of the same iteration (primary oracle, N=96 personas), one point per training iteration (label = iteration), per arm, all four arms. Dashed = per-arm least squares, corner text = raw Spearman rho; the partial-correlation version is in pref_outcome_correlations. GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `training_signal_yield.png` — How much usable training signal each iteration produced, all four arms: groups that trained, the yield rate, and how decisive those groups were (best-worst training-reward gap); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `update_category_by_arm.png` — Each MI-concept word group projected onto the per-iteration update direction (+ = the update favours that concept's words), one line per arm, all four arms. Per-iteration directions: read alongside update_direction_quality, whose split_half_cos says how much of each curve is estimation noise; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `update_lexical_push.png` — What each training iteration's update pushes toward, all four arms on one scale: completion length, question marks, affirmation and over-praise markers (+/-1 SE over groups; + = favours more of the feature); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).

**Tables** — `tables/gpt-4o-mini/`
- `GRPO_LA0_examples.md` — GRPO_LA0: the 3 groups per iteration with the largest training-reward gap, first and last training iteration, as text (training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable)). Illustration of what the update was choosing between -- deliberately the most decisive groups, NOT a random sample, so read them as exhibits and never as a rate.
- `GRPO_LA5_examples.md` — GRPO_LA5: the 3 groups per iteration with the largest training-reward gap, first and last training iteration, as text (training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable)). Illustration of what the update was choosing between -- deliberately the most decisive groups, NOT a random sample, so read them as exhibits and never as a rate.
- `PTO_LA0_examples.md` — PTO_LA0: the 3 groups per iteration with the largest training-reward gap, first and last training iteration, as text (training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable)). Illustration of what the update was choosing between -- deliberately the most decisive groups, NOT a random sample, so read them as exhibits and never as a rate.
- `PTO_LA0_pref_MI_concepts.md` — PTO_LA0 MI-concept projection onto the preference direction per training iteration (+ = the concept's words are chosen-aligned); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA0_pref_probe_quality.md` — PTO_LA0 preference-probe quality per training iteration (wins_correct, gap, margin) over the tau-filtered pairs.csv pairs; unit = one emitted pair; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). wins_correct is IN-SAMPLE (optimistic) -- the honest wins_holdout is in update_direction_quality.
- `PTO_LA5_examples.md` — PTO_LA5: the 3 groups per iteration with the largest training-reward gap, first and last training iteration, as text (training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable)). Illustration of what the update was choosing between -- deliberately the most decisive groups, NOT a random sample, so read them as exhibits and never as a rate.
- `PTO_LA5_pref_MI_concepts.md` — PTO_LA5 MI-concept projection onto the preference direction per training iteration (+ = the concept's words are chosen-aligned); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `PTO_LA5_pref_probe_quality.md` — PTO_LA5 preference-probe quality per training iteration (wins_correct, gap, margin) over the tau-filtered pairs.csv pairs; unit = one emitted pair; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). wins_correct is IN-SAMPLE (optimistic) -- the honest wins_holdout is in update_direction_quality.
- `generation_pool_means.md` — Per (arm, training iteration), all four arms: the mean of each lexical feature over ALL candidates -- what the policy GENERATES, as opposed to what the update selects for. NOTE the indexing: train_iter n samples from the iter-start policy, so this row describes the same policy the eval set calls model_iter_{n-1}; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `pref_outcome_correlations.md` — Spearman rho between each update feature and the eval delta it preceded, per arm and pooled per method (a pooled row pools that method's K=0 and K=5 arms). spearman_rho is the raw association; rho_feature_vs_iter shows how much the feature simply trends with training; rho_partial_iter is the one to read (train_iter partialled out of both sides). Descriptive: n <= 10 iterations per arm, no multiplicity correction; unit = one training iteration; eval side primary oracle only.
- `pref_outcome_link.md` — Per (arm, train_iter, metric), all four arms: every feature of that iteration's update next to the persona-paired eval delta it produced (model_iter_n minus model_iter_n-1; delta_mean/dz/p from compare_two_models, paired on persona_id, N=96; + = the later policy scores higher, MICI lower = better). The training-signal -> eval-move join. Training features from the training oracle gpt-4o-mini; eval side graded by the primary oracle only. GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `training_signal_yield.md` — Per (arm, training iteration), all four arms: groups_built (branch points / prompt groups logged), groups_trained (those with both an up- and a down-weighted side), yield_rate, and the best-worst training-reward gap. GRPO trains on essentially every group; PTO's tau filter drops the branch points whose branches tie; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `update_direction_cosines.md` — Cosine between every pair of arms' POOLED update directions (all six arm pairs), with the attenuation ceiling sqrt(r_a*r_b) from each direction's Spearman-Brown-corrected split-half reliability and the corrected cosine. 1.0 corrected = the two updates pull toward the same language; the ceiling is why a raw cosine of 0.3 is not interpretable on its own; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable).
- `update_direction_quality.md` — Per (arm, training iteration) probe audit of the update direction, all four arms: in-sample wins_correct vs the honest wins_holdout (each half scored by the other half's direction), mean projection gap, and split_half_cos = cosine between directions fitted on disjoint halves (the precision check; halves drawn with BOOT_SEED per (arm, iteration)). A low split_half_cos means the direction is real but unmeasured at this group count, NOT that the update has no target; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `update_direction_quality_pooled.md` — The same audit for the per-ARM direction pooled over all training iterations, all four arms -- the estimate cross-method claims should rest on, since pooling multiplies the group count and the split-half cosine rises accordingly; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `update_lexical_push.md` — Per (arm, training iteration), all four arms: the lexical contrast the update pushes for, Sum(w*feature) per group +/- SE, on a shared scale for both methods (DPO's +/-1 pair; GRPO's standardized advantages rescaled to match). + = the update favours MORE of the feature; 0 = indifferent. Uses EVERY gradient group (no embedding, no sampling); training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).
- `weighting_decomposition.md` — Cosine between update directions under three conditions, one block per matched-K PTO/GRPO pair (K = 0 and K = 5): as trained (rule AND data differ), same groups with the rule swapped (the RULE's effect), and same rule on each method's own groups (the DATA's effect). Read the `read` column: the attenuation ceiling assumes independent estimation error, which holds across arms but NOT for the same-groups rows, where both directions share their noise and the correction over-corrects (values above 1.0 are the tell). Pooled over training iterations; training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable). GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).

**Number ledgers** — `tables/gpt-4o-mini/`
- `preference_numbers.json` — Number ledger for arms/preference: per-arm pooled probe audit (wins_holdout, split_half_cos), attenuation-corrected cosines between the arms' pooled update directions, the as-trained PTO-vs-GRPO decomposition cosine per K, the pairs.csv sanity cosine, and the training-signal yield anchors. Training side only (training oracle gpt-4o-mini (partial-conv candidate rewards; not judge-swappable)); primary leaf only. GRPO K=5 stopped after iteration 5 (censored; its training rows end at train_iter 5).

## arms/stats

**Tables** — `tables/claude-haiku-4-5/`
- `friedman_omnibus.md` — Friedman repeated-measures omnibus across iterations per arm x rubric (chi2, p, Kendall's W), all four arms; grader = claude-haiku-4-5 (held-out judge). Unit = persona (persona_id x iteration pivot, base included; n_personas = complete rows). No sign convention (an omnibus). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations. Thin arms dropped.
- `main_results.md` — FINAL and BEST iteration vs base per arm x metric (column `target`), all four arms; grader = claude-haiku-4-5 (held-out judge). persona-paired (persona_id, N=96 shared personas; never file_index): delta = target - base (+ => trained model higher; MICI is lower-better so + is worse there), paired Cohen's dz + label, Wilcoxon p (Holm across rubrics within arm x target), bootstrap 95% CI (BOOT_SEED), trajectory Spearman rho / OLS slope. BEST = the arm's peak iteration on its own training oracle (best_per_experiment). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations. Thin arms (<3 scored iters) dropped.
- `rubric_pca_pc1.md` — Variance explained by PC1 of the standardized rubric scores per arm (canonical 10-metric factor space, pooled over every conversation x iteration of the arm), all four arms; grader = claude-haiku-4-5 (held-out judge). A dominant PC1 => the rubrics ~ one latent factor, so 'every metric up' is weak evidence of multi-skill gain. Unit = conversation; no pairing, no sign. GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations.
- `slope_by_arm.md` — Per-iteration OLS slope + Spearman rho per arm x metric (climb rate; peak_iter vs final_iter flags a post-peak regression), all four arms; grader = claude-haiku-4-5 (held-out judge). Pooled persona x iteration rows (unit = conversation), so NO p is reported -- descriptive; the repeated-measures omnibus is friedman_omnibus. + slope => the metric climbs with iteration (MICI is lower-better, so + is worse there). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations. Thin arms dropped.
- `vs_base_paired.md` — Each arm x iteration vs its OWN base on Q1+Q2 -- one merged table (column `arm`), all four arms; grader = claude-haiku-4-5 (held-out judge). persona-paired (persona_id, N=96 shared personas; never file_index): mean_delta = iteration - base (+ => later policy higher), Wilcoxon p, dz, Holm p (across iterations WITHIN each arm), bootstrap 95% CI (BOOT_SEED). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations.

**Tables** — `tables/gpt-4o-mini/`
- `friedman_omnibus.md` — Friedman repeated-measures omnibus across iterations per arm x rubric (chi2, p, Kendall's W), all four arms; grader = gpt-4o-mini (primary oracle = the training reward). Unit = persona (persona_id x iteration pivot, base included; n_personas = complete rows). No sign convention (an omnibus). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations. Thin arms dropped.
- `main_results.md` — FINAL and BEST iteration vs base per arm x metric (column `target`), all four arms; grader = gpt-4o-mini (primary oracle = the training reward). persona-paired (persona_id, N=96 shared personas; never file_index): delta = target - base (+ => trained model higher; MICI is lower-better so + is worse there), paired Cohen's dz + label, Wilcoxon p (Holm across rubrics within arm x target), bootstrap 95% CI (BOOT_SEED), trajectory Spearman rho / OLS slope. BEST = the arm's peak iteration on its own training oracle (best_per_experiment). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations. Thin arms (<3 scored iters) dropped.
- `rubric_pca_pc1.md` — Variance explained by PC1 of the standardized rubric scores per arm (canonical 10-metric factor space, pooled over every conversation x iteration of the arm), all four arms; grader = gpt-4o-mini (primary oracle = the training reward). A dominant PC1 => the rubrics ~ one latent factor, so 'every metric up' is weak evidence of multi-skill gain. Unit = conversation; no pairing, no sign. GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations.
- `slope_by_arm.md` — Per-iteration OLS slope + Spearman rho per arm x metric (climb rate; peak_iter vs final_iter flags a post-peak regression), all four arms; grader = gpt-4o-mini (primary oracle = the training reward). Pooled persona x iteration rows (unit = conversation), so NO p is reported -- descriptive; the repeated-measures omnibus is friedman_omnibus. + slope => the metric climbs with iteration (MICI is lower-better, so + is worse there). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations. Thin arms dropped.
- `vs_base_paired.md` — Each arm x iteration vs its OWN base on Q1+Q2 -- one merged table (column `arm`), all four arms; grader = gpt-4o-mini (primary oracle = the training reward). persona-paired (persona_id, N=96 shared personas; never file_index): mean_delta = iteration - base (+ => later policy higher), Wilcoxon p, dz, Holm p (across iterations WITHIN each arm), bootstrap 95% CI (BOOT_SEED). GRPO_LA5 was trained to iteration 5 (the other arms to 10), so its final/last rows are iteration 5 and its trajectory statistics span 6 iterations.
