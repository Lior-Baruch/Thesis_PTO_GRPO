# Exp3 results — START HERE

Hand-authored navigation for the results tree. **If you want a number, this page tells you which
table owns it; if you want the story, each `<top>/SUMMARY.md` tells it.** The auto-generated maps
are [`INDEX.md`](INDEX.md) (counts per family) and each top's `INDEX.md` (every artifact with its
caption). This page, the five `SUMMARY.md`, [`METRICS_REFERENCE.md`](METRICS_REFERENCE.md),
[`LIMITATIONS.md`](LIMITATIONS.md) and [`schematics/`](schematics/) are hand-authored and survive
every re-render; everything else is regenerated by `python tools/render_results.py` from `eda/`.

> ⚠ **Tables are the evidence; prose (including this page) is interpretation.** Quote numbers from
> the tables, never from a summary — see CLAUDE.md § "Epistemic status of these docs".

## The tree in one line each

| top | question | narrative |
|---|---|---|
| [`arms/`](arms/) | what did each of the four arms do? (per-arm descriptives, one `<judge>/` leaf per grader) | [`arms/SUMMARY.md`](arms/SUMMARY.md) |
| [`lookahead/`](lookahead/) | RQ-i — does K=5 beat K=0, within each optimizer? | [`lookahead/SUMMARY.md`](lookahead/SUMMARY.md) |
| [`method/`](method/) | RQ-ii — PTO vs GRPO at each K | [`method/SUMMARY.md`](method/SUMMARY.md) |
| [`compute/`](compute/) | every lever again, at matched GPU-hours instead of matched iteration | [`compute/SUMMARY.md`](compute/SUMMARY.md) |
| [`measurement/`](measurement/) | is the ruler trustworthy? (two graders, ICC, saturation) | [`measurement/SUMMARY.md`](measurement/SUMMARY.md) |

## The headline artifacts, by question

**Where do the four arms land?** —
[`method/contrast/figures/headline_grid.png`](method/contrast/figures/headline_grid.png) +
[`tables/headline_grid.md`](method/contrast/tables/headline_grid.md): every arm's endpoint on
every rubric under both graders, each arm anchored to its own base, persona-bootstrap CIs. The
rubric-by-rubric leaderboard per grader is
[`arms/outcomes/tables/<judge>/leaderboard_scorecard.md`](arms/outcomes/tables/gpt-4o-mini/leaderboard_scorecard.md);
the per-metric learning curves are `arms/outcomes/figures/<judge>/trajectories/`.

**The method verdict (PTO vs GRPO) — an interaction with K, never a ranking.** —
[`method/contrast/tables/method_paired_by_K.md`](method/contrast/tables/method_paired_by_K.md)
(sign: **+ = PTO higher**) is the main table, cited by every other SUMMARY. At iteration 10 the
sign flips between K=0 (PTO wins) and K=5 (GRPO wins) on both graders. Steelman:
[`method_paired_best.md`](method/contrast/tables/method_paired_best.md); trajectory:
[`figures/method_gap.png`](method/contrast/figures/method_gap.png).

**The look-ahead lever (K=0 vs K=5).** — By-iteration contrast:
[`lookahead/reward/tables/k_table1.md`](lookahead/reward/tables/k_table1.md) (**sign: + = K=0
higher**); endpoint pairs with both graders:
[`k_endpoints.md`](lookahead/reward/tables/k_endpoints.md) (sign as named in its `pair` column);
difference-in-differences: [`k_did.md`](lookahead/reward/tables/k_did.md). Figures:
[`k_headline_q1q2.png`](lookahead/reward/figures/k_headline_q1q2.png) (four arms),
[`k_headline_q1q2_grpo.png`](lookahead/reward/figures/k_headline_q1q2_grpo.png) (GRPO only —
⚠ its contrast row plots **K5 − K0**, the opposite of the tables, so up = look-ahead winning).

**The reward hack / over-praise story.** — Judge-free evidence first:
[`lookahead/behaviour/figures/overpraise_judgefree.png`](lookahead/behaviour/figures/overpraise_judgefree.png)
(the deterministic lexical marker beside both graders' rated rates; backing numbers in
[`overpraise_judgefree_data.md`](lookahead/behaviour/tables/overpraise_judgefree_data.md)); the
per-arm cross-check table is
[`arms/validity/tables/<judge>/overpraise_crosscheck.md`](arms/validity/tables/gpt-4o-mini/overpraise_crosscheck.md);
channel composition and substitution:
[`lookahead/behaviour/tables/k_mici_composition.md`](lookahead/behaviour/tables/k_mici_composition.md).

**What a run cost, and who wins at matched budget.** —
[`compute/cost/tables/compute_by_arm.md`](compute/cost/tables/compute_by_arm.md) (GPU-h; mtime
reconstruction, never `iteration_metadata.json`); **quote the budget sweep, never one iso-compute
row**: `budget_sweep_<contrast>_<judge>.md` ×8 and the honest-selection verdicts in
[`budget_sweep_crossjudge_verdicts.md`](compute/cost/tables/budget_sweep_crossjudge_verdicts.md);
figure [`budget_sweep.png`](compute/cost/figures/budget_sweep.png). API axis:
[`api_calls.md`](compute/cost/tables/api_calls.md) / [`api_ratio.md`](compute/cost/tables/api_ratio.md).

**Can the numbers be trusted?** — Coverage:
[`measurement/validity/tables/multijudge_coverage.md`](measurement/validity/tables/multijudge_coverage.md);
arm-level sign preservation:
[`multijudge_sign_preservation.md`](measurement/validity/tables/multijudge_sign_preservation.md);
the per-conversation agreement collapse and its saturation mechanism:
[`figures/judge_saturation.png`](measurement/validity/figures/judge_saturation.png) +
[`tables/judge_saturation_data.md`](measurement/validity/tables/judge_saturation_data.md); oracle
repeatability: [`oracle_repeatability_icc.md`](measurement/validity/tables/oracle_repeatability_icc.md).

**Why (mechanism) — mostly negative results, stated as such.** — Reward faithfulness under K
(read [`METRICS_REFERENCE.md`](METRICS_REFERENCE.md) §6a FIRST — the two cuts answer different
questions): [`lookahead/mechanism/tables/faithfulness_k_summary.md`](lookahead/mechanism/tables/faithfulness_k_summary.md);
dispersion (rescaling, not sharpening):
[`dispersion_ratios.md`](lookahead/mechanism/tables/dispersion_ratios.md); the K-step tail audit:
[`tail_audit_by_iter.md`](lookahead/mechanism/tables/tail_audit_by_iter.md); the ICLR replication:
[`lookahead/replication/tables/crossgen_kcontrast.md`](lookahead/replication/tables/crossgen_kcontrast.md).

## Reading rules (the ones that prevent wrong numbers)

1. **Sign conventions differ per table and are stated in each caption.** `k_table1` / `k_paired_*`
   / `k_summary` report **K0 − K5**; `method_paired_*` report **PTO − GRPO**; `k_endpoints` names
   its pair per row; the GRPO-only headline figure plots **K5 − K0**. Say which you are quoting.
2. **Never average the two graders, never compare levels across them** (offset 1.2–1.7 points,
   model-dependent). Contrasts and standardized quantities only.
3. **Name the axis**: matched-iteration vs matched-budget disagree at K=5, and behaviour rates
   have three denominators (per turn / per session / share of coded acts — prefer the share).
4. **Pair on `persona_id`, never `file_index`** — personas reshuffle every iteration.
5. **MICI is lower-is-better**; MITI is the least dependable instrument — lead with Q1+Q2.
6. Every `tables/` leaf also holds a `.xlsx` workbook (one sheet per table) and every leaf a
   `CAPTIONS.md`; number ledgers are `tables/*.json` with `{value, source, note}` per key.

## Regenerating

```
cd Exp3_PTO_GRPO/eda
python tools/render_results.py            # everything (deterministic; BOOT_SEED everywhere)
python -m eda_analysis._selfcheck         # 26 checks, run after any EDA change
```

Hand-authored files (this page, `SUMMARY.md`s, `METRICS_REFERENCE.md`, `LIMITATIONS.md`,
`schematics/`) are in `exports.PRESERVE` and are never touched by a render.
