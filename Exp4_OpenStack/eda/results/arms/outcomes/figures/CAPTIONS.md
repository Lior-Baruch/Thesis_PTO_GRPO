- **endpoint_best_gpt4m** -- Per-persona Q1Q2 at each arm's OWN best model state, grader gpt4m. Box = median, diamond = mean with an unpaired 95% bootstrap CI (seed=BOOT_SEED), dotted line = the untrained base level, one per setting. The same 96 personas are in every box, so overlap here does not bound a paired difference.
- **endpoint_final_gpt4m** -- Per-persona Q1Q2 at each arm's OWN final model state, grader gpt4m. Box = median, diamond = mean with an unpaired 95% bootstrap CI (seed=BOOT_SEED), dotted line = the untrained base level, one per setting. The same 96 personas are in every box, so overlap here does not bound a paired difference.
- **trajectory_gpt4m** -- Mean score by MODEL STATE, one panel per instrument, grader gpt4m. State 0 is the untrained base; the dotted line is the base level pooled over the arms of one setting, one line per setting (each starts from its own untrained policy). Bands are unpaired 95% bootstrap CIs across the 96 personas (seed=BOOT_SEED) -- overlap between two bands is NOT evidence of no difference. MICI is lower-is-better.
