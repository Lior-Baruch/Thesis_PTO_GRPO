% Figure 4 belongs to sec:measurement; it is placed here in the source only so that it is
% encountered in a left-hand column and lands at the top of the page sec:measurement occupies.
\begin{figure*}[t]
\centering
\includegraphics[width=0.86\textwidth]{figures/judge_saturation_grpo.png}
\caption{\textbf{(a)} Per-conversation cross-grader agreement on Q1 by iteration, one line per
arm, against the 22-state median. \textbf{(b)} The spread of each grader's Q1 scores along each arm
(SD is a spread, not a level, so the graders can share the axis): the training oracle's spread
falls as its level rises and re-expands where the level falls; the held-out judge's does not move.
\textbf{(c)} The share of conversations scored $4.5$ or above on Q1: the training oracle is at its
ceiling along $\Kf$; the held-out judge never approaches it.}
\label{fig:saturation}
\end{figure*}

\section{Why it works, as far as we can tell}
\label{sec:mechanism}

Three mechanisms were testable in this design, and none is confirmed
(Appendix~\ref{app:mechanism} has the full analysis). \emph{A more faithful reward?} The training
reward scores a partial conversation while the evaluation scores the whole session. Pooled over
the run, the $\Kf$ signal ranks candidates more like the full-session score does (pairwise rank
agreement $0.909$ vs.\ $0.873$ under the training oracle, $0.800$ vs.\ $0.747$ held out), but the
iteration-level test does not clear $.05$ ($p = .084$ / $.193$), and at the one matched-policy
cut (iteration 1, both arms branching from the same base) both deltas straddle zero: unsupported.
\emph{Sharper group discrimination?} Look-ahead widens the best-minus-worst margin within a group
($1.300\times$), but the within-group standard deviation grows by the same factor ($1.293\times$;
ratio of ratios $1.006$ $[1.002, 1.010]$) and GRPO divides by it: a rescaling the normalisation
absorbs, so look-ahead changes \emph{which} candidate the group prefers, not how confidently.
\emph{A different update direction?} The two arms' advantage-weighted sentence-embedding directions
have a cosine of $0.804$ ($0.851$ after correcting for the split-half reliability ceiling): barely.

What remains is that the same optimiser, pushed in nearly the same direction by a reward that is
not measurably better conditioned or more faithful at a fixed policy, ends further from base by a
margin that survives the $\Kz$ arm's best checkpoint. What differs is which of eight siblings is
preferred at each step, a small, repeated difference compounded over ten iterations of a loop that
regenerates its training data from the policy it just updated. Isolating that pathway needs the
horizon swapped mid-run on one policy, a cut this design lacks.
